import initialState from "@src/redux/initialstate/home";

function reducerHome (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducerHome;
